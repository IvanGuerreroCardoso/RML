This project is a simple app created in react native for managing lists, specifically of rateable media.

To run this project just clone the repo and install deps (npm install)
then start metro:
`npm start`
once metro is up, run:
`npm run android`